server:
  port: 7003
  servlet:
    context-path: /coupon-settlement

spring:
  application:
    name: eureka-client-coupon-settlement

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server1:8001/eureka/

logging:
  level:
    com.dongliu.coupon: debug
